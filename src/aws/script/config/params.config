# - 修正内容
#   - HTTP_PROXY_HOST/HTTP_PROXY_PORT（オプション）
#     - インストール先のAWSがプロキシ環境下の場合に指定します。
#     - 作業PCの環境は関係ありません。
#     - プロキシ情報はネットワーク管理者に確認してください。
#   - AWS_REGION（必須）
#     - インストール先のAWSのリージョンを指定します。
#   - AWS_SNS_TOPIC（必須）
#     - SNSのトピックのARNを指定します。
#     - バックアップ前後のアプリ停止/開始失敗時のメール送信先に使用します。
#     - AWSマネジメントコンソールでSNSにアクセスし、トピックのARNを確認します。
#       - ![SNSのトピックARN](images/aws-sns-topicarn.png)
#   - EC2_USER_CQ_PASS/EC2_USER_CI_PASS/EC2_USER_DEMO_PASS（必須）
#     - 各インスタンスのec2-userユーザのパスワードを指定します。
#     - 各インスタンス間のSSH接続に使用します。

HTTP_PROXY_HOST=
HTTP_PROXY_PORT=
AWS_REGION=ap-northeast-1
AWS_SNS_TOPIC=
EC2_USER_CQ_PASS=
EC2_USER_CI_PASS=
EC2_USER_DEMO_PASS=